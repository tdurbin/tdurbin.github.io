<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Secure Chat Area</title>
    <style>
        body { font-family: sans-serif; padding: 50px; }
        .status { padding: 20px; background: #e0f7fa; border-radius: 5px; margin-bottom: 20px; }
    </style>

    <!-- BEGIN LivePerson Monitor. -->
    <script type="text/javascript">window.lpTag=window.lpTag||{},'undefined'==typeof window.lpTag._tagCount?(window.lpTag={wl:lpTag.wl||null,scp:lpTag.scp||null,site:'81412129'||'',section:lpTag.section||'',tagletSection:lpTag.tagletSection||null,autoStart:lpTag.autoStart!==!1,ovr:lpTag.ovr||{},_v:'1.10.0',_tagCount:1,protocol:'https:',events:{bind:function(t,e,i){lpTag.defer(function(){lpTag.events.bind(t,e,i)},0)},trigger:function(t,e,i){lpTag.defer(function(){lpTag.events.trigger(t,e,i)},1)}},defer:function(t,e){0===e?(this._defB=this._defB||[],this._defB.push(t)):1===e?(this._defT=this._defT||[],this._defT.push(t)):(this._defL=this._defL||[],this._defL.push(t))},load:function(t,e,i){var n=this;setTimeout(function(){n._load(t,e,i)},0)},_load:function(t,e,i){var n=t;t||(n=this.protocol+'//'+(this.ovr&&this.ovr.domain?this.ovr.domain:'lptag.liveperson.net')+'/tag/tag.js?site='+this.site);var o=document.createElement('script');o.setAttribute('charset',e?e:'UTF-8'),i&&o.setAttribute('id',i),o.setAttribute('src',n),document.getElementsByTagName('head').item(0).appendChild(o)},init:function(){this._timing=this._timing||{},this._timing.start=(new Date).getTime();var t=this;window.attachEvent?window.attachEvent('onload',function(){t._domReady('domReady')}):(window.addEventListener('DOMContentLoaded',function(){t._domReady('contReady')},!1),window.addEventListener('load',function(){t._domReady('domReady')},!1)),'undefined'===typeof window._lptStop&&this.load()},start:function(){this.autoStart=!0},_domReady:function(t){this.isDom||(this.isDom=!0,this.events.trigger('LPT','DOM_READY',{t:t})),this._timing[t]=(new Date).getTime()},vars:lpTag.vars||[],dbs:lpTag.dbs||[],ctn:lpTag.ctn||[],sdes:lpTag.sdes||[],hooks:lpTag.hooks||[],identities:lpTag.identities||[],ev:lpTag.ev||[]},lpTag.init()):window.lpTag._tagCount+=1;</script>
    <!-- END LivePerson Monitor. -->

</head>
<body>

    <h1>Welcome to the Member Area</h1>
    
    <div id="status" class="status">Checking authentication...</div>
    <button onclick="logout()">Log Out</button>

    <script>
        // 1. Parse the URL to get the 'code' sent by Auth0
        const urlParams = new URLSearchParams(window.location.search);
        const authCode = urlParams.get('code');

        const statusDiv = document.getElementById('status');

        if (authCode) {
            statusDiv.innerHTML = `<strong>Success!</strong> Auth Code received: <code>${authCode.substring(0,10)}...</code><br>Connecting to LivePerson...`;
            
            // CLEAN URL (Optional): Remove the code from the address bar so the user doesn't see it
            // window.history.replaceState({}, document.title, window.location.pathname);
            
        } else {
            statusDiv.innerHTML = "No credentials found. Redirecting to login...";
            setTimeout(() => { window.location.href = 'index.html'; }, 2000);
        }

        // 2. Define the LivePerson Authentication Hook
        // This function is called AUTOMATICALLY by the LivePerson SDK when the chat window opens
        // window.lpGetAuthenticationToken = function(callback) {
        //var lpGetAuthenticationToken = function(callback) {
        //    console.log("LivePerson is asking for the token...");
            
        //    if (authCode) {
                // Pass the code we grabbed from the URL to LivePerson
        //        callback(authCode);
                //callback({
                //    ssoKey: authCode,
                //    redirect_uri: "https://tdurbin.github.io/auth/chat.html" // Must match the value used in index.html
                //});
         //   } else {
                // If the chat is opened but the user isn't logged in, send them to login
         //       console.log("User not logged in, cannot start authenticated chat.");
         //       callback(null); // Or redirect to login
         //   }
        //};

        // 3. LOGOUT FUNCTION
        function logout() {
            // Replace with your domain and client ID
            const domain = "dev-0ds2lps500xulygz.uk.auth0.com";
            const clientId = "etyzcqmFckYtsLOH3HhLieK6yRNy254z";
            const returnTo = "https://tdurbin.github.io/auth/index.html";
            
            window.location.href = `https://${domain}/v2/logout?client_id=${clientId}&returnTo=${encodeURIComponent(returnTo)}`;
        }
    </script>

    </body>
</html>